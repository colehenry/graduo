import React from "react";
import Navigator from "./src/Navigator";

// This is the base file for our app. All we want to do here is tell it to use our navigator.
export default function App() {
  return <Navigator />;
}
